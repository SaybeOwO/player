<%= @user.id %>

<%= form_with(model: temp) do |form| %>
  <% if temp.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(temp.errors.count, "error") %> prohibited this temp from being saved:</h2>

      <ul>
        <% temp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :link, style: "display: block" %>
    <%= form.text_field :link %>
  </div>  

  <div>
    <%= form.hidden_field :uid, value: 1 %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
